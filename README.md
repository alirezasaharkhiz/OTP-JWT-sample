# OTP Auth Service

✅ OTP-based authentication with Gin + Redis + Postgres

### Features
- Login/Register with OTP
- User info in Postgres
- OTP in Redis with TTL
- JWT token after login
- Rate limiting (to be added)
- Swagger docs

### Run locally
```bash
docker-compose up --build
